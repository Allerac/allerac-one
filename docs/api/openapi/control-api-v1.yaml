openapi: 3.1.0
info:
  title: Allerac Control API
  version: 1.0.0-beta
  description: >
    Control API v1 beta contract for Allerac One. The current implementation supports
    browser session authentication and bearer API keys. The version endpoint is
    intentionally public for deployment verification.
servers:
  - url: http://localhost:8080
    description: Local Docker deployment
security:
  - sessionCookie: []
  - bearerApiKey: []
tags:
  - name: System
  - name: Capabilities
  - name: API Keys
  - name: Conversations
  - name: Memories
  - name: Tickets
  - name: Agent Runs
  - name: Documents
  - name: Notes
  - name: Scheduled Jobs
  - name: Skills
  - name: Health
  - name: Search
  - name: Robot
  - name: Email
  - name: Finance
  - name: Music
  - name: Benchmark
  - name: Crawler
paths:
  /api/v1/crawler/sources:
    put:
      tags: [Crawler]
      summary: Create or update a crawler source
      operationId: upsertCrawlerSource
      responses:
        "200": { description: Source saved }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/crawler/runs:
    post:
      tags: [Crawler]
      summary: Queue a crawler run
      operationId: createCrawlerRun
      responses:
        "202": { description: Run queued }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/crawler/runs/claim:
    post:
      tags: [Crawler]
      summary: Atomically claim the next crawler run
      operationId: claimCrawlerRun
      responses:
        "200": { description: Leased run and source configuration }
        "204": { description: No eligible work }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/crawler/runs/{runId}:
    patch:
      tags: [Crawler]
      summary: Update crawler run state
      operationId: updateCrawlerRun
      parameters:
        - { name: runId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Run updated }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/UnprocessableEntity" }
  /api/v1/crawler/runs/{runId}/heartbeat:
    post:
      tags: [Crawler]
      summary: Renew a crawler run lease
      operationId: heartbeatCrawlerRun
      parameters:
        - { name: runId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Lease renewed }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/UnprocessableEntity" }
  /api/v1/crawler/runs/{runId}/events:
    post:
      tags: [Crawler]
      summary: Append an operational crawler event
      operationId: createCrawlerRunEvent
      parameters:
        - { name: runId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Event stored }
        "401": { $ref: "#/components/responses/Unauthorized" }
  /api/v1/crawler/runs/{runId}/documents-upsert:
    post:
      tags: [Crawler]
      summary: Idempotently ingest normalized crawler documents
      operationId: upsertCrawlerDocuments
      parameters:
        - { name: runId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: Idempotency-Key, in: header, required: true, schema: { type: string, minLength: 8 } }
      responses:
        "200": { description: Durable document acknowledgement }
        "400": { $ref: "#/components/responses/ValidationError" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/UnprocessableEntity" }
  /api/v1/version:
    get:
      tags: [System]
      summary: Get deployed release identity
      operationId: getApiVersion
      security: []
      responses:
        "200":
          description: Release and build identity served by this application instance
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store, max-age=0
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VersionResponse"
  /api/v1/me:
    get:
      tags: [System]
      summary: Get current API user
      operationId: getApiMe
      responses:
        "200":
          description: Current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MeResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/domains:
    get:
      tags: [System]
      summary: List accessible domains
      operationId: listDomains
      responses:
        "200":
          description: Active domains visible to the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DomainListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/capabilities:
    get:
      tags: [Capabilities]
      summary: Get effective capabilities
      operationId: getCapabilities
      description: Returns safe availability metadata for integrations and providers visible to the authenticated user. Secrets are never returned.
      responses:
        "200":
          description: Effective capability map
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CapabilitiesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/api-keys:
    get:
      tags: [API Keys]
      summary: List API keys
      operationId: listApiKeys
      responses:
        "200":
          description: API keys owned by the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiKeyListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [API Keys]
      summary: Create API key
      operationId: createApiKey
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateApiKeyRequest"
      responses:
        "201":
          description: Created API key. The secret is returned only once.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateApiKeyResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/api-keys/{id}:
    delete:
      tags: [API Keys]
      summary: Revoke API key
      operationId: revokeApiKey
      parameters:
        - $ref: "#/components/parameters/ApiKeyId"
      responses:
        "200":
          description: API key revoked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RevokeApiKeyResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/conversations:
    get:
      tags: [Conversations]
      summary: List conversations
      operationId: listConversations
      parameters:
        - name: domainSlug
          in: query
          required: false
          schema:
            type: string
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Conversations owned by the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationListResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags: [Conversations]
      summary: Create conversation
      operationId: createConversation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateConversationRequest"
      responses:
        "201":
          description: Created conversation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/conversations/{id}/messages:
    get:
      tags: [Conversations]
      summary: List conversation messages
      operationId: listConversationMessages
      parameters:
        - $ref: "#/components/parameters/ConversationId"
      responses:
        "200":
          description: Messages in an owned conversation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ConversationMessagesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags: [Conversations]
      summary: Send conversation message
      description: Sends a user message and returns the final assistant response synchronously.
      operationId: sendConversationMessage
      parameters:
        - $ref: "#/components/parameters/ConversationId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendConversationMessageRequest"
      responses:
        "201":
          description: Assistant response generated and persisted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendConversationMessageResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
        "429":
          description: Chat rate or concurrency limit exceeded
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /api/v1/conversations/{id}/memory:
    post:
      tags: [Memories]
      summary: Create memory from conversation
      operationId: createConversationMemory
      parameters:
        - $ref: "#/components/parameters/ConversationId"
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateConversationMemoryRequest"
      responses:
        "201":
          description: Created or refreshed conversation memory
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /api/v1/memories:
    get:
      tags: [Memories]
      summary: List memories
      operationId: listMemories
      parameters:
        - name: domainSlug
          in: query
          required: false
          schema:
            type: string
        - $ref: "#/components/parameters/Limit"
        - name: minImportance
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 10
        - name: query
          in: query
          required: false
          description: Case-insensitive text search across memory summaries and topics.
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Memories owned by the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryListResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/memories/{id}:
    delete:
      tags: [Memories]
      summary: Delete memory
      operationId: deleteMemory
      parameters:
        - $ref: "#/components/parameters/MemoryId"
      responses:
        "200":
          description: Memory deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteMemoryResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/tickets:
    get:
      tags: [Tickets]
      summary: List tickets
      operationId: listTickets
      parameters:
        - $ref: "#/components/parameters/TicketStatus"
        - $ref: "#/components/parameters/TicketType"
        - $ref: "#/components/parameters/PriorityLevel"
        - $ref: "#/components/parameters/Limit"
        - $ref: "#/components/parameters/Offset"
      responses:
        "200":
          description: Tickets owned by the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketListResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags: [Tickets]
      summary: Create ticket
      operationId: createTicket
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateTicketRequest"
      responses:
        "201":
          description: Created ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/tickets/{id}:
    get:
      tags: [Tickets]
      summary: Get ticket
      operationId: getTicket
      parameters:
        - $ref: "#/components/parameters/TicketId"
      responses:
        "200":
          description: Ticket plus event timeline
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketDetailResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags: [Tickets]
      summary: Update ticket
      operationId: updateTicket
      parameters:
        - $ref: "#/components/parameters/TicketId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateTicketRequest"
      responses:
        "200":
          description: Updated ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Tickets]
      summary: Delete ticket
      operationId: deleteTicket
      parameters:
        - $ref: "#/components/parameters/TicketId"
      responses:
        "200":
          description: Ticket deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeleteTicketResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/tickets/{id}/events:
    get:
      tags: [Tickets]
      summary: List ticket events
      operationId: listTicketEvents
      parameters:
        - $ref: "#/components/parameters/TicketId"
      responses:
        "200":
          description: Ticket event timeline
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TicketEventsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/agent-runs:
    get:
      tags: [Agent Runs]
      summary: List agent runs
      operationId: listAgentRuns
      parameters:
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Agent runs owned by the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentRunListResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags: [Agent Runs]
      summary: Create agent run
      operationId: createAgentRun
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateAgentRunRequest"
      responses:
        "201":
          description: Created pending agent run
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentRunResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/agent-runs/{id}:
    get:
      tags: [Agent Runs]
      summary: Get agent run
      operationId: getAgentRun
      parameters:
        - $ref: "#/components/parameters/AgentRunId"
      responses:
        "200":
          description: Agent run with worker status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AgentRunResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/agent-runs/{id}/cancel:
    post:
      tags: [Agent Runs]
      summary: Cancel agent run
      operationId: cancelAgentRun
      parameters:
        - $ref: "#/components/parameters/AgentRunId"
      responses:
        "200":
          description: Agent run cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CancelAgentRunResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/documents:
    get:
      tags: [Documents]
      summary: List documents
      operationId: listDocuments
      parameters:
        - name: domainSlug
          in: query
          required: false
          schema:
            type: string
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Documents owned by the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentListResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Documents]
      summary: Upload document
      operationId: uploadDocument
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                  description: Max 20 MB
                domainSlug:
                  type: string
      responses:
        "201":
          description: Document created, processing in background
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/documents/{id}:
    delete:
      tags: [Documents]
      summary: Delete document
      operationId: deleteDocument
      parameters:
        - $ref: "#/components/parameters/DocumentId"
      responses:
        "200":
          description: Document and all chunks deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeletedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/notes:
    get:
      tags: [Notes]
      summary: List notes
      operationId: listNotes
      parameters:
        - name: tag
          in: query
          required: false
          schema:
            type: string
        - name: due_on
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: due_before
          in: query
          required: false
          schema:
            type: string
            format: date
        - name: overdue
          in: query
          required: false
          schema:
            type: boolean
        - $ref: "#/components/parameters/Limit"
      responses:
        "200":
          description: Notes owned by the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteListResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Notes]
      summary: Create note
      operationId: createNote
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateNoteRequest"
      responses:
        "201":
          description: Created note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/notes/search:
    get:
      tags: [Notes]
      summary: Search notes
      operationId: searchNotes
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
            minLength: 1
      responses:
        "200":
          description: Matching notes ranked by relevance
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteSearchResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/notes/tags:
    get:
      tags: [Notes]
      summary: List note tags
      operationId: listNoteTags
      responses:
        "200":
          description: All distinct tags used in the user's notes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteTagsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/notes/{id}:
    patch:
      tags: [Notes]
      summary: Update note
      operationId: updateNote
      parameters:
        - $ref: "#/components/parameters/NoteId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateNoteRequest"
      responses:
        "200":
          description: Updated note
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NoteResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Notes]
      summary: Delete note
      operationId: deleteNote
      parameters:
        - $ref: "#/components/parameters/NoteId"
      responses:
        "200":
          description: Note deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeletedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/jobs:
    get:
      tags: [Scheduled Jobs]
      summary: List scheduled jobs
      operationId: listJobs
      responses:
        "200":
          description: Scheduled jobs owned by the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Scheduled Jobs]
      summary: Create scheduled job
      operationId: createJob
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateJobRequest"
      responses:
        "201":
          description: Created scheduled job
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/jobs/{id}:
    patch:
      tags: [Scheduled Jobs]
      summary: Update scheduled job
      operationId: updateJob
      parameters:
        - $ref: "#/components/parameters/JobId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateJobRequest"
      responses:
        "200":
          description: Updated scheduled job
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Scheduled Jobs]
      summary: Delete scheduled job
      operationId: deleteJob
      parameters:
        - $ref: "#/components/parameters/JobId"
      responses:
        "200":
          description: Job and execution history deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeletedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/jobs/{id}/toggle:
    post:
      tags: [Scheduled Jobs]
      summary: Toggle job enabled state
      operationId: toggleJob
      parameters:
        - $ref: "#/components/parameters/JobId"
      responses:
        "200":
          description: Updated job with toggled enabled state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/jobs/{id}/run:
    post:
      tags: [Scheduled Jobs]
      summary: Run scheduled job now
      operationId: runJob
      parameters:
        - $ref: "#/components/parameters/JobId"
      responses:
        "201":
          description: Job executed immediately and execution record returned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobExecutionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/jobs/{id}/executions:
    get:
      tags: [Scheduled Jobs]
      summary: List job executions
      operationId: listJobExecutions
      parameters:
        - $ref: "#/components/parameters/JobId"
      responses:
        "200":
          description: Execution history for the job
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobExecutionListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/skills:
    get:
      tags: [Skills]
      summary: List skills
      operationId: listSkills
      responses:
        "200":
          description: Skills available to the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Skills]
      summary: Create skill
      operationId: createSkill
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateSkillRequest"
      responses:
        "201":
          description: Created skill
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/skills/{id}:
    get:
      tags: [Skills]
      summary: Get skill
      operationId: getSkill
      parameters:
        - $ref: "#/components/parameters/SkillId"
      responses:
        "200":
          description: Skill including full system prompt
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillDetailResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    patch:
      tags: [Skills]
      summary: Update skill
      operationId: updateSkill
      parameters:
        - $ref: "#/components/parameters/SkillId"
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UpdateSkillRequest"
      responses:
        "200":
          description: Updated skill
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SkillResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Skills]
      summary: Delete skill
      operationId: deleteSkill
      parameters:
        - $ref: "#/components/parameters/SkillId"
      responses:
        "200":
          description: Skill deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DeletedResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/health/status:
    get:
      tags: [Health]
      summary: Get Garmin connection status
      operationId: getHealthStatus
      responses:
        "200":
          description: Garmin connection status for the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/health/summary:
    get:
      tags: [Health]
      summary: Get health summary
      operationId: getHealthSummary
      parameters:
        - name: period
          in: query
          required: false
          schema:
            type: string
            enum: [day, 3days, week, month, year]
            default: week
      responses:
        "200":
          description: Aggregated health metrics for the requested period
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthSummaryResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/health/daily:
    get:
      tags: [Health]
      summary: Get daily health snapshot
      operationId: getDailyHealth
      parameters:
        - name: date
          in: query
          required: false
          schema:
            type: string
            format: date
            description: Defaults to today
      responses:
        "200":
          description: Full health metrics for a single day
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyHealthResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/health/activities:
    get:
      tags: [Health]
      summary: List activities
      operationId: listHealthActivities
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 10
        - name: date
          in: query
          required: false
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Recent Garmin activities
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthActivitiesResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/health/activities/{activityId}:
    get:
      tags: [Health]
      summary: Get one activity's normalized detail
      operationId: getHealthActivity
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Normalized detail for one activity, including detail_sync_status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthActivityDetailResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Activity not found
    delete:
      tags: [Health]
      summary: Delete an activity and all derived data
      operationId: deleteHealthActivity
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Activity deleted (laps/zones/samples cascade)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthActivityDeleteResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Activity not found
  /api/v1/health/activities/{activityId}/laps:
    get:
      tags: [Health]
      summary: List laps for one activity
      operationId: listHealthActivityLaps
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Laps for one activity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthActivityLapsResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Activity not found
  /api/v1/health/activities/{activityId}/zones:
    get:
      tags: [Health]
      summary: List heart-rate/power time-in-zone aggregates for one activity
      operationId: listHealthActivityZones
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Time-in-zone aggregates for one activity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthActivityZonesResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Activity not found
  /api/v1/health/activities/{activityId}/sync:
    post:
      tags: [Health]
      summary: Queue a detail sync for one activity
      operationId: syncHealthActivity
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
      responses:
        "202":
          description: Detail sync queued; processed asynchronously
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthActivitySyncResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Activity not found
  /api/v1/health/activities/{activityId}/route:
    get:
      tags: [Health]
      summary: Get route bounds, simplified polyline, and optionally redacted detailed coordinates
      operationId: getHealthActivityRoute
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
        - name: detail
          in: query
          required: false
          schema:
            type: string
            enum: ["true", "false"]
            default: "false"
      responses:
        "200":
          description: Route bounds/polyline, plus redacted coordinates when detail=true
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthActivityRouteResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Activity not found
  /api/v1/health/activities/{activityId}/series:
    get:
      tags: [Health]
      summary: Get a bounded, downsampled time series for selected metrics
      operationId: getHealthActivitySeries
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
        - name: metrics
          in: query
          required: false
          schema:
            type: string
            description: Comma-separated metric names; defaults to all
        - name: maxPoints
          in: query
          required: false
          schema:
            type: integer
            minimum: 10
            maximum: 5000
            default: 500
      responses:
        "200":
          description: Bounded, redacted, downsampled time series
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthActivitySeriesResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Activity not found
  /api/v1/health/protected-locations:
    get:
      tags: [Health]
      summary: List privacy zones
      operationId: listHealthProtectedLocations
      responses:
        "200":
          description: The caller's privacy zones
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthProtectedLocationsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Health]
      summary: Add a privacy zone
      operationId: addHealthProtectedLocation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/HealthProtectedLocationInput"
      responses:
        "201":
          description: Privacy zone created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthProtectedLocationResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/health/protected-locations/{id}:
    delete:
      tags: [Health]
      summary: Remove a privacy zone
      operationId: deleteHealthProtectedLocation
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Privacy zone deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthProtectedLocationDeleteResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          description: Protected location not found
  /api/v1/health/proxy/status:
    get:
      tags: [Health]
      summary: Live Garmin connection status (proxy mode, unstored)
      operationId: getHealthProxyStatus
      responses:
        "200":
          description: Garmin connection status, read live — no health_activities/health_daily_metrics access
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthProxyStatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/health/proxy/activities:
    get:
      tags: [Health]
      summary: List activities live from Garmin (proxy mode, unstored)
      operationId: getHealthProxyActivities
      parameters:
        - name: date
          in: query
          required: true
          schema:
            type: string
            format: date
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
      responses:
        "200":
          description: Activities fetched live from Garmin for the given date — the raw provider objects, never written to health_activities
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthProxyActivitiesResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Garmin is not connected for this user
  /api/v1/health/proxy/activities/{activityId}:
    get:
      tags: [Health]
      summary: Live laps/zones/route/details for one activity (proxy mode, unstored)
      operationId: getHealthProxyActivityDetail
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
            pattern: "^[0-9]+$"
      responses:
        "200":
          description: >-
            The health-worker's raw activity-details payload, untouched — laps, zones,
            samples (with exact GPS coordinates), route bounds/polyline, and details_raw
            (Garmin's complete unreduced payload). No column whitelist and no redaction,
            unlike the cached /activities/{activityId} family.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthProxyActivityDetailResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Garmin is not connected for this user
  /api/v1/health/proxy/activities/{activityId}/laps:
    get:
      tags: [Health]
      summary: Live laps for one activity (proxy mode, unstored)
      operationId: getHealthProxyActivityLaps
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
            pattern: "^[0-9]+$"
      responses:
        "200":
          description: >-
            The health-worker's raw laps array — an independent live call, same shape as
            /activities/{activityId} but sliced to just laps.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthProxyActivityDetailResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Garmin is not connected for this user
  /api/v1/health/proxy/activities/{activityId}/zones:
    get:
      tags: [Health]
      summary: Live heart-rate/power zones for one activity (proxy mode, unstored)
      operationId: getHealthProxyActivityZones
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
            pattern: "^[0-9]+$"
      responses:
        "200":
          description: >-
            The health-worker's raw zones array — an independent live call, same shape as
            /activities/{activityId} but sliced to just zones.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthProxyActivityDetailResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Garmin is not connected for this user
  /api/v1/health/proxy/activities/{activityId}/route:
    get:
      tags: [Health]
      summary: Live route bounds/polyline and GPS samples for one activity (proxy mode, unstored)
      operationId: getHealthProxyActivityRoute
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
            pattern: "^[0-9]+$"
      responses:
        "200":
          description: >-
            route_bounds, route_simplified_polyline, and the full raw samples array
            (exact GPS included, unredacted) — an independent live call. Same samples
            array as .../series, since Garmin's raw payload doesn't split the two.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthProxyActivityDetailResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Garmin is not connected for this user
  /api/v1/health/proxy/activities/{activityId}/series:
    get:
      tags: [Health]
      summary: Live time-series samples for one activity (proxy mode, unstored)
      operationId: getHealthProxyActivitySeries
      parameters:
        - name: activityId
          in: path
          required: true
          schema:
            type: string
            pattern: "^[0-9]+$"
      responses:
        "200":
          description: >-
            The raw samples array — an independent live call. Includes GPS coordinates,
            unlike the cached /series endpoint; identical to .../route's samples.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthProxyActivityDetailResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Garmin is not connected for this user
  /api/v1/health/proxy/daily:
    get:
      tags: [Health]
      summary: Live daily health snapshot (proxy mode, unstored)
      operationId: getHealthProxyDaily
      parameters:
        - name: date
          in: query
          required: true
          schema:
            type: string
            format: date
      responses:
        "200":
          description: Daily health metrics fetched live from Garmin — the raw provider payload, never written to health_daily_metrics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthProxyDailyResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "409":
          description: Garmin is not connected for this user
  /api/v1/search:
    get:
      tags: [Search]
      summary: Search the web
      operationId: searchWeb
      parameters:
        - name: q
          in: query
          required: true
          schema:
            type: string
            minLength: 1
            maxLength: 500
      responses:
        "200":
          description: Tavily-backed web search result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WebSearchResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /api/v1/speech:
    post:
      tags: [Robot]
      summary: Synthesize speech audio
      operationId: synthesizeSpeech
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SpeechRequest"
      responses:
        "200":
          description: MPEG audio containing the synthesized speech
          headers:
            Cache-Control:
              schema:
                type: string
                const: no-store
          content:
            audio/mpeg:
              schema:
                type: string
                format: binary
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /api/v1/robot/settings:
    get:
      tags: [Robot]
      summary: Get robot assistant settings and tool availability
      operationId: getRobotSettings
      responses:
        "200":
          description: Robot assistant settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RobotSettingsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    put:
      tags: [Robot]
      summary: Update robot assistant speech settings
      operationId: updateRobotSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RobotSettingsUpdateRequest"
      responses:
        "200":
          description: Updated robot assistant speech settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RobotSettingsUpdateResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/email/messages:
    get:
      tags: [Email]
      summary: List email messages
      operationId: listEmailMessages
      parameters:
        - name: accountId
          in: query
          required: true
          schema:
            type: string
        - name: sinceUid
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          description: Recent messages for an owned email account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailMessageListResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/email/message:
    get:
      tags: [Email]
      summary: Get email message
      operationId: getEmailMessage
      parameters:
        - name: accountId
          in: query
          required: true
          schema:
            type: string
        - name: uid
          in: query
          required: true
          schema:
            type: integer
            minimum: 1
      responses:
        "200":
          description: Email message detail for an owned account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmailMessageResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/email/send:
    post:
      tags: [Email]
      summary: Send email
      operationId: sendEmail
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SendEmailRequest"
      responses:
        "200":
          description: Email accepted by SMTP transport
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SendEmailResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/finance/quote:
    get:
      tags: [Finance]
      summary: Get market quotes or search symbols
      operationId: getFinanceQuote
      parameters:
        - name: symbols
          in: query
          required: false
          schema:
            type: string
          description: Comma-separated ticker symbols. Required unless `q` is provided.
        - name: q
          in: query
          required: false
          schema:
            type: string
          description: Symbol search query. Required unless `symbols` is provided.
      responses:
        "200":
          description: Quote list or symbol search results
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/FinanceQuotesResponse"
                  - $ref: "#/components/schemas/FinanceSymbolSearchResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/finance/candles:
    get:
      tags: [Finance]
      summary: Get market candles
      operationId: getFinanceCandles
      parameters:
        - name: symbol
          in: query
          required: true
          schema:
            type: string
        - name: period
          in: query
          required: false
          schema:
            type: string
            enum: [1W, 1M, 6M]
            default: 1M
      responses:
        "200":
          description: Candle close series for the requested symbol and period
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FinanceCandlesResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/finance/watchlist:
    get:
      tags: [Finance]
      summary: Get watchlist
      operationId: getWatchlist
      responses:
        "200":
          description: Symbols on the authenticated user's watchlist
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WatchlistResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
    post:
      tags: [Finance]
      summary: Add symbol to watchlist
      operationId: addToWatchlist
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddWatchlistSymbolRequest"
      responses:
        "201":
          description: Symbol added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WatchlistSymbolResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /api/v1/finance/watchlist/{symbol}:
    delete:
      tags: [Finance]
      summary: Remove symbol from watchlist
      operationId: removeFromWatchlist
      parameters:
        - name: symbol
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Symbol removed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WatchlistSymbolResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /api/v1/music/status:
    get:
      tags: [Music]
      summary: Get Spotify connection status
      operationId: getMusicStatus
      responses:
        "200":
          description: Spotify connection status for the authenticated user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MusicStatusResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/music/recommendations:
    get:
      tags: [Music]
      summary: Get personalized track recommendations
      operationId: getMusicRecommendations
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 30
      responses:
        "200":
          description: Recommendations from the local content-based recommender
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MusicRecommendationsResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/music/top-tracks:
    get:
      tags: [Music]
      summary: Get most-listened tracks for a time window
      operationId: getMusicTopTracks
      parameters:
        - name: period
          in: query
          required: false
          schema:
            type: string
            enum: [top_short, top_medium, top_long]
            default: top_medium
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
      responses:
        "200":
          description: Top tracks for the requested window
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MusicTopTracksResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/music/recently-played:
    get:
      tags: [Music]
      summary: Get recently played tracks
      operationId: getMusicRecentlyPlayed
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 20
      responses:
        "200":
          description: Most recently played tracks
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MusicRecentlyPlayedResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/music/playlists:
    get:
      tags: [Music]
      summary: List the user's Spotify playlists
      operationId: listMusicPlaylists
      responses:
        "200":
          description: Live playlist list from the Spotify Web API
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MusicPlaylistsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
    post:
      tags: [Music]
      summary: Create a Spotify playlist
      operationId: createMusicPlaylist
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateMusicPlaylistRequest"
      responses:
        "201":
          description: Playlist created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CreateMusicPlaylistResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /api/v1/music/playlists/{id}/tracks:
    get:
      tags: [Music]
      summary: List tracks inside a playlist
      operationId: listMusicPlaylistTracks
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Spotify playlist id
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 100
      responses:
        "200":
          description: Tracks inside the requested playlist
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MusicPlaylistTracksResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
    post:
      tags: [Music]
      summary: Add tracks to a playlist
      operationId: addMusicPlaylistTracks
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
          description: Spotify playlist id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddMusicPlaylistTracksRequest"
      responses:
        "201":
          description: Tracks added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AddMusicPlaylistTracksResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /api/v1/music/sync:
    post:
      tags: [Music]
      summary: Trigger a full Spotify sync
      operationId: syncMusic
      responses:
        "200":
          description: Sync completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MusicSyncResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          $ref: "#/components/responses/UnprocessableEntity"
  /api/v1/benchmark/models:
    get:
      tags: [Benchmark]
      summary: List benchmark models and current availability
      operationId: listBenchmarkModels
      responses:
        "200":
          description: Model catalog and provider availability
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BenchmarkModelListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags: [Memories]
      summary: Create a manual memory
      operationId: createManualMemory
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateManualMemoryRequest"
      responses:
        "201":
          description: Manual memory created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MemoryResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
  /api/v1/benchmark/embeddings:
    get:
      tags: [Benchmark]
      summary: List embedding benchmark models and installation state
      operationId: listEmbeddingBenchmarkModels
      responses:
        "200":
          description: Embedding model candidates and their Ollama installation state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmbeddingBenchmarkModelListResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags: [Benchmark]
      summary: Compare local embedding models
      operationId: runEmbeddingBenchmark
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EmbeddingBenchmarkRunRequest"
      responses:
        "200":
          description: Embedding benchmark measurements for each selected model
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EmbeddingBenchmarkRunResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "429":
          description: Benchmark concurrency limit reached
  /api/v1/benchmark/runs:
    get:
      tags: [Benchmark]
      summary: List benchmark runs owned by the authenticated user
      operationId: listBenchmarkRuns
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 50
            default: 5
      responses:
        "200":
          description: Benchmark history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BenchmarkRunListResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
    post:
      tags: [Benchmark]
      summary: Run the standard benchmark suite
      operationId: runBenchmark
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BenchmarkRunRequest"
      responses:
        "200":
          description: Server-sent event stream containing benchmark progress and results
          content:
            text/event-stream:
              schema:
                type: string
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"
        "422":
          description: Selected provider is not configured
        "429":
          description: Benchmark concurrency limit reached
    delete:
      tags: [Benchmark]
      summary: Delete all benchmark history owned by the authenticated user
      operationId: clearBenchmarkRuns
      responses:
        "200":
          description: History deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "403":
          $ref: "#/components/responses/Forbidden"

components:
  securitySchemes:
    sessionCookie:
      type: apiKey
      in: cookie
      name: session_token
    bearerApiKey:
      type: http
      scheme: bearer
      description: Control API key using the `alr_live_` token format.
  parameters:
    ApiKeyId:
      name: id
      in: path
      required: true
      schema:
        type: string
    ConversationId:
      name: id
      in: path
      required: true
      schema:
        type: string
    MemoryId:
      name: id
      in: path
      required: true
      schema:
        type: string
    TicketId:
      name: id
      in: path
      required: true
      schema:
        type: string
    AgentRunId:
      name: id
      in: path
      required: true
      schema:
        type: string
    DocumentId:
      name: id
      in: path
      required: true
      schema:
        type: string
    NoteId:
      name: id
      in: path
      required: true
      schema:
        type: string
    JobId:
      name: id
      in: path
      required: true
      schema:
        type: string
    SkillId:
      name: id
      in: path
      required: true
      schema:
        type: string
    TicketStatus:
      name: status
      in: query
      required: false
      schema:
        $ref: "#/components/schemas/TicketStatus"
    TicketType:
      name: type
      in: query
      required: false
      schema:
        $ref: "#/components/schemas/TicketType"
    PriorityLevel:
      name: priorityLevel
      in: query
      required: false
      schema:
        $ref: "#/components/schemas/PriorityLevel"
    Limit:
      name: limit
      in: query
      required: false
      schema:
        type: integer
        minimum: 1
        maximum: 100
    Offset:
      name: offset
      in: query
      required: false
      schema:
        type: integer
        minimum: 0
  responses:
    Unauthorized:
      description: No valid authentication
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    Forbidden:
      description: Authenticated but not allowed
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    NotFound:
      description: Resource not found or not owned by the user
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    ValidationError:
      description: Invalid request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
    UnprocessableEntity:
      description: Request is valid but cannot be processed in the current state
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorResponse"
  schemas:
    VersionResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [release, commit, builtAt]
          properties:
            release:
              type: string
            commit:
              type: string
            builtAt:
              type: string
    MeResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [user]
          properties:
            user:
              $ref: "#/components/schemas/ApiUser"
    ApiUser:
      type: object
      required: [id, email, name, isAdmin, authMode]
      properties:
        id:
          type: string
        email:
          type: string
          format: email
        name:
          type: [string, "null"]
        isAdmin:
          type: boolean
        authMode:
          type: string
          enum: [session, api_key]
    CapabilitiesResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [capabilities, defaults]
          properties:
            capabilities:
              $ref: "#/components/schemas/CapabilityMap"
            defaults:
              type: object
              required: [chatModel]
              properties:
                chatModel:
                  type: [string, "null"]
    CapabilityMap:
      type: object
      required: [llm, search, notifications, storage, social, email, health]
      properties:
        llm:
          type: object
          required: [github, gemini, anthropic, ollama]
          properties:
            github:
              $ref: "#/components/schemas/CapabilityStatus"
            gemini:
              $ref: "#/components/schemas/CapabilityStatus"
            anthropic:
              $ref: "#/components/schemas/CapabilityStatus"
            ollama:
              $ref: "#/components/schemas/CapabilityStatus"
        search:
          type: object
          required: [tavily]
          properties:
            tavily:
              $ref: "#/components/schemas/CapabilityStatus"
        notifications:
          type: object
          required: [telegram, resend]
          properties:
            telegram:
              $ref: "#/components/schemas/CapabilityStatus"
            resend:
              $ref: "#/components/schemas/CapabilityStatus"
        storage:
          type: object
          required: [azureBlob]
          properties:
            azureBlob:
              $ref: "#/components/schemas/CapabilityStatus"
        social:
          type: object
          required: [instagram, tiktok]
          properties:
            instagram:
              $ref: "#/components/schemas/CapabilityStatus"
            tiktok:
              $ref: "#/components/schemas/CapabilityStatus"
        email:
          type: object
          required: [imap, smtp]
          properties:
            imap:
              $ref: "#/components/schemas/CapabilityStatus"
            smtp:
              $ref: "#/components/schemas/CapabilityStatus"
        health:
          type: object
          required: [garmin]
          properties:
            garmin:
              $ref: "#/components/schemas/CapabilityStatus"
    CapabilityStatus:
      type: object
      required: [configured, available]
      properties:
        configured:
          type: boolean
        available:
          type: boolean
        connected:
          type: boolean
    ApiKeyListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [apiKeys]
          properties:
            apiKeys:
              type: array
              items:
                $ref: "#/components/schemas/ApiKey"
    CreateApiKeyRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 100
        scopes:
          type: array
          items:
            type: string
        expiresAt:
          type: [string, "null"]
          format: date-time
    CreateApiKeyResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [apiKey, secret]
          properties:
            apiKey:
              $ref: "#/components/schemas/ApiKey"
            secret:
              type: string
              description: Full API key secret. Returned only once.
    RevokeApiKeyResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [revoked]
          properties:
            revoked:
              type: boolean
              const: true
    ApiKey:
      type: object
      required: [id, name, prefix, scopes, lastUsedAt, revokedAt, expiresAt, createdAt]
      properties:
        id:
          type: string
        name:
          type: string
        prefix:
          type: string
        scopes:
          type: array
          items:
            type: string
        lastUsedAt:
          type: [string, "null"]
          format: date-time
        revokedAt:
          type: [string, "null"]
          format: date-time
        expiresAt:
          type: [string, "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time
    DomainListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [domains]
          properties:
            domains:
              type: array
              items:
                $ref: "#/components/schemas/Domain"
    Domain:
      type: object
      required: [slug, displayName, isActive, defaultSkill]
      properties:
        slug:
          type: string
        displayName:
          type: string
        isActive:
          type: boolean
        defaultSkill:
          anyOf:
            - $ref: "#/components/schemas/DomainDefaultSkill"
            - type: "null"
    DomainDefaultSkill:
      type: object
      required: [id, name, displayName]
      properties:
        id:
          type: string
        name:
          type: string
        displayName:
          type: [string, "null"]
    ConversationListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [conversations]
          properties:
            conversations:
              type: array
              items:
                $ref: "#/components/schemas/Conversation"
    ConversationResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [conversation]
          properties:
            conversation:
              $ref: "#/components/schemas/Conversation"
    ConversationMessagesResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [messages]
          properties:
            messages:
              type: array
              items:
                $ref: "#/components/schemas/ConversationMessage"
    SendConversationMessageRequest:
      type: object
      required: [model, provider]
      properties:
        message:
          type: string
          maxLength: 100000
        model:
          type: string
          minLength: 1
          maxLength: 200
        provider:
          type: string
          enum: [github, ollama, gemini, anthropic]
        imageAttachments:
          type: array
          maxItems: 5
          items:
            type: object
            required: [url]
            properties:
              url:
                type: string
                maxLength: 8388608
                description: Must be a data:image/* URL or https URL.
        preSelectedSkillId:
          type: string
          format: uuid
        defaultSkillName:
          type: string
        postContext:
          type: string
          maxLength: 20000
      anyOf:
        - required: [message]
        - required: [imageAttachments]
    SendConversationMessageResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [message, events]
          properties:
            message:
              type: object
              required: [conversationId, role, content]
              properties:
                conversationId:
                  type: string
                role:
                  type: string
                  const: assistant
                content:
                  type: string
            events:
              type: array
              items:
                $ref: "#/components/schemas/ChatExecutionEvent"
    ChatExecutionEvent:
      type: object
      required: [type]
      properties:
        type:
          type: string
          examples: [token, tool_call, tool_result, skill_activated]
      additionalProperties: true
    CreateConversationRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 200
        domainSlug:
          type: string
    Conversation:
      type: object
      required: [id, title, domainSlug, pinned, createdAt, updatedAt]
      properties:
        id:
          type: string
        title:
          type: string
        domainSlug:
          type: [string, "null"]
        pinned:
          type: boolean
        createdAt:
          type: [string, "null"]
          format: date-time
        updatedAt:
          type: [string, "null"]
          format: date-time
    ConversationMessage:
      type: object
      required: [id, conversationId, role, content, agentRunId, createdAt]
      properties:
        id:
          type: string
        conversationId:
          type: string
        role:
          type: string
          enum: [system, user, assistant, tool]
        content:
          type: string
        agentRunId:
          type: [string, "null"]
        createdAt:
          type: [string, "null"]
          format: date-time
    CreateConversationMemoryRequest:
      type: object
      additionalProperties: false
      properties:
        importanceScore:
          type: integer
          minimum: 1
          maximum: 10
          description: Optional manual override for model-generated importance.
        emotion:
          type: [integer, "null"]
          enum: [-1, 0, 1, null]
          description: Optional UI-aligned sentiment override. -1 is frustrated/negative, 0 is neutral, 1 is happy/positive.
    CreateManualMemoryRequest:
      type: object
      additionalProperties: false
      required: [content]
      properties:
        content:
          type: string
          minLength: 1
          maxLength: 10000
        keyTopics:
          type: array
          maxItems: 20
          items:
            type: string
            minLength: 1
            maxLength: 100
        importanceScore:
          type: integer
          minimum: 1
          maximum: 10
          default: 7
        emotion:
          type: [integer, "null"]
          enum: [-1, 0, 1, null]
        domainSlug:
          type: string
          description: Accessible target domain. Defaults to chat.
    MemoryListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [memories]
          properties:
            memories:
              type: array
              items:
                $ref: "#/components/schemas/Memory"
    MemoryResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [memory]
          properties:
            memory:
              $ref: "#/components/schemas/Memory"
    DeleteMemoryResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [deleted]
          properties:
            deleted:
              type: boolean
              const: true
    Memory:
      type: object
      required:
        - id
        - conversationId
        - userId
        - summary
        - keyTopics
        - importanceScore
        - messageCount
        - domainSlug
        - emotion
        - createdAt
      properties:
        id:
          type: string
        conversationId:
          type: [string, "null"]
        userId:
          type: string
        summary:
          type: string
        keyTopics:
          type: array
          items:
            type: string
        importanceScore:
          type: integer
          minimum: 1
          maximum: 10
        messageCount:
          type: integer
          minimum: 0
        domainSlug:
          type: [string, "null"]
        emotion:
          type: [string, "null"]
        createdAt:
          type: string
          format: date-time
    TicketListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [tickets]
          properties:
            tickets:
              type: array
              items:
                $ref: "#/components/schemas/Ticket"
    TicketResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [ticket]
          properties:
            ticket:
              $ref: "#/components/schemas/Ticket"
    TicketDetailResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [ticket, events]
          properties:
            ticket:
              $ref: "#/components/schemas/Ticket"
            events:
              type: array
              items:
                $ref: "#/components/schemas/TicketEvent"
    TicketEventsResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [events]
          properties:
            events:
              type: array
              items:
                $ref: "#/components/schemas/TicketEvent"
    DeleteTicketResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [deleted]
          properties:
            deleted:
              type: boolean
              const: true
    AgentRunListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [agentRuns]
          properties:
            agentRuns:
              type: array
              items:
                $ref: "#/components/schemas/AgentRun"
    AgentRunResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [agentRun]
          properties:
            agentRun:
              $ref: "#/components/schemas/AgentRun"
    CancelAgentRunResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [cancelled]
          properties:
            cancelled:
              type: boolean
              const: true
    CreateAgentRunRequest:
      type: object
      required: [message]
      properties:
        message:
          type: string
          minLength: 1
        conversationId:
          type: [string, "null"]
          format: uuid
        model:
          type: string
        provider:
          type: string
        skillName:
          type: string
    AgentRun:
      type: object
      required:
        - id
        - conversationId
        - userId
        - status
        - prompt
        - plan
        - result
        - error
        - model
        - provider
        - skillId
        - lastHeartbeat
        - startedAt
        - completedAt
        - cancelledAt
        - workers
      properties:
        id:
          type: string
        conversationId:
          type: string
        userId:
          type: string
        status:
          type: string
          enum: [pending, planning, running, aggregating, completed, failed, cancelled]
        prompt:
          type: string
        plan:
          type: [object, "null"]
          additionalProperties: true
        result:
          type: [string, "null"]
        error:
          type: [string, "null"]
        model:
          type: [string, "null"]
        provider:
          type: [string, "null"]
        skillId:
          type: [string, "null"]
        lastHeartbeat:
          type: [string, "null"]
          format: date-time
        startedAt:
          type: [string, "null"]
          format: date-time
        completedAt:
          type: [string, "null"]
          format: date-time
        cancelledAt:
          type: [string, "null"]
          format: date-time
        workers:
          type: array
          items:
            $ref: "#/components/schemas/AgentWorker"
    AgentWorker:
      type: object
      required:
        - id
        - name
        - task
        - skillId
        - status
        - result
        - tokensUsed
        - progressLog
        - lastHeartbeat
        - startedAt
        - completedAt
      properties:
        id:
          type: string
        name:
          type: string
        task:
          type: string
        skillId:
          type: [string, "null"]
        status:
          type: string
          enum: [waiting, running, completed, failed]
        result:
          type: [string, "null"]
        tokensUsed:
          type: [integer, "null"]
        progressLog:
          type: [string, "null"]
        lastHeartbeat:
          type: [string, "null"]
          format: date-time
        startedAt:
          type: [string, "null"]
          format: date-time
        completedAt:
          type: [string, "null"]
          format: date-time
    CreateTicketRequest:
      type: object
      required: [title]
      properties:
        title:
          type: string
          minLength: 1
        description:
          type: string
        type:
          $ref: "#/components/schemas/TicketType"
        explicitUrgency:
          $ref: "#/components/schemas/PriorityLevel"
        tags:
          type: array
          items:
            type: string
        context:
          type: object
          additionalProperties: true
    UpdateTicketRequest:
      type: object
      properties:
        status:
          $ref: "#/components/schemas/TicketStatus"
        resolutionNotes:
          type: string
        resolvedByType:
          type: string
          enum: [user, agent]
        assignedToType:
          type: [string, "null"]
          enum: [user, agent, null]
        contextPatch:
          type: object
          additionalProperties: true
    Ticket:
      type: object
      required:
        - id
        - number
        - title
        - description
        - type
        - status
        - priority
        - createdBy
        - assignedTo
        - resolvedBy
        - resolutionNotes
        - tags
        - context
        - createdAt
        - updatedAt
        - resolvedAt
        - cancelledAt
      properties:
        id:
          type: string
        number:
          type: integer
        title:
          type: string
        description:
          type: [string, "null"]
        type:
          $ref: "#/components/schemas/TicketType"
        status:
          $ref: "#/components/schemas/TicketStatus"
        priority:
          $ref: "#/components/schemas/TicketPriority"
        createdBy:
          $ref: "#/components/schemas/TicketActorRef"
        assignedTo:
          anyOf:
            - $ref: "#/components/schemas/AssignmentRef"
            - type: "null"
        resolvedBy:
          anyOf:
            - $ref: "#/components/schemas/TicketActorRef"
            - type: "null"
        resolutionNotes:
          type: [string, "null"]
        tags:
          type: array
          items:
            type: string
        context:
          type: [object, "null"]
          additionalProperties: true
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
        resolvedAt:
          type: [string, "null"]
          format: date-time
        cancelledAt:
          type: [string, "null"]
          format: date-time
    TicketEvent:
      type: object
      required:
        - id
        - ticketId
        - type
        - actor
        - previousValue
        - newValue
        - notes
        - createdAt
      properties:
        id:
          type: string
        ticketId:
          type: string
        type:
          type: string
          examples: [created, status_changed, resolved, deleted]
        actor:
          $ref: "#/components/schemas/EventActorRef"
        previousValue:
          type: [object, "null"]
          additionalProperties: true
        newValue:
          type: [object, "null"]
          additionalProperties: true
        notes:
          type: [string, "null"]
        createdAt:
          type: string
          format: date-time
    TicketPriority:
      type: object
      required: [level, score, factors]
      properties:
        level:
          $ref: "#/components/schemas/PriorityLevel"
        score:
          type: integer
        factors:
          type: [object, "null"]
          additionalProperties: true
    TicketActorRef:
      type: object
      required: [type, runId]
      properties:
        type:
          type: string
          enum: [user, agent]
        runId:
          type: [string, "null"]
    AssignmentRef:
      type: object
      required: [type]
      properties:
        type:
          type: string
          enum: [user, agent]
    EventActorRef:
      type: object
      required: [type, runId]
      properties:
        type:
          type: string
          enum: [user, agent, system]
        runId:
          type: [string, "null"]
    TicketStatus:
      type: string
      enum: [open, in_progress, resolved, cancelled]
    TicketType:
      type: string
      enum: [task, bug, improvement, question]
    PriorityLevel:
      type: string
      enum: [low, medium, high, critical]
    DeletedResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [deleted]
          properties:
            deleted:
              type: boolean
              const: true
    DocumentListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [documents]
          properties:
            documents:
              type: array
              items:
                $ref: "#/components/schemas/Document"
    DocumentResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [document]
          properties:
            document:
              $ref: "#/components/schemas/Document"
    Document:
      type: object
      required: [id, filename, fileType, fileSize, domainSlug, status, errorMessage, uploadedAt]
      properties:
        id:
          type: string
        filename:
          type: string
        fileType:
          type: string
        fileSize:
          type: integer
        domainSlug:
          type: [string, "null"]
        status:
          type: string
          enum: [processing, completed, failed]
        errorMessage:
          type: [string, "null"]
        uploadedAt:
          type: string
          format: date-time
    NoteListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [notes]
          properties:
            notes:
              type: array
              items:
                $ref: "#/components/schemas/Note"
    NoteResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [note]
          properties:
            note:
              $ref: "#/components/schemas/Note"
    NoteSearchResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [results]
          properties:
            results:
              type: array
              items:
                $ref: "#/components/schemas/NoteSearchResult"
    NoteTagsResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [tags]
          properties:
            tags:
              type: array
              items:
                type: string
    CreateNoteRequest:
      type: object
      required: [content]
      properties:
        content:
          type: string
          minLength: 1
        title:
          type: string
        tags:
          type: array
          items:
            type: string
        source:
          type: string
        due_date:
          type: [string, "null"]
          format: date
    UpdateNoteRequest:
      type: object
      properties:
        content:
          type: string
        title:
          type: [string, "null"]
        tags:
          type: array
          items:
            type: string
        due_date:
          type: [string, "null"]
          format: date
    Note:
      type: object
      required: [id, title, content, tags, source, dueDate, createdAt, updatedAt]
      properties:
        id:
          type: string
        title:
          type: [string, "null"]
        content:
          type: string
        tags:
          type: array
          items:
            type: string
        source:
          type: string
        dueDate:
          type: [string, "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    NoteSearchResult:
      type: object
      required: [id, title, content, tags, similarity, createdAt]
      properties:
        id:
          type: string
        title:
          type: [string, "null"]
        content:
          type: string
        tags:
          type: array
          items:
            type: string
        similarity:
          type: number
          minimum: 0
          maximum: 1
        createdAt:
          type: string
          format: date-time
    JobListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [jobs]
          properties:
            jobs:
              type: array
              items:
                $ref: "#/components/schemas/ScheduledJob"
    JobResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [job]
          properties:
            job:
              $ref: "#/components/schemas/ScheduledJob"
    JobExecutionListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [executions]
          properties:
            executions:
              type: array
              items:
                $ref: "#/components/schemas/JobExecution"
    JobExecutionResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [execution]
          properties:
            execution:
              $ref: "#/components/schemas/JobExecution"
    BenchmarkRunRequest:
      type: object
      required: [model, provider]
      properties:
        model:
          type: string
          minLength: 1
          maxLength: 200
        provider:
          type: string
          enum: [github, ollama, gemini, anthropic]
    EmbeddingBenchmarkRunRequest:
      type: object
      required: [models]
      properties:
        models:
          type: array
          minItems: 1
          maxItems: 3
          items:
            type: string
            minLength: 1
            maxLength: 200
    EmbeddingBenchmarkModelListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [models]
          properties:
            models:
              type: array
              items:
                type: object
                required: [id, installed]
                properties:
                  id: { type: string }
                  installed: { type: boolean }
    EmbeddingBenchmarkRunResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [run]
          properties:
            run:
              type: object
              required: [id, createdAt, results]
              properties:
                id: { type: string, format: uuid }
                createdAt: { type: string, format: date-time }
                results:
                  type: array
                  items:
                    oneOf:
                      - $ref: "#/components/schemas/EmbeddingBenchmarkSuccess"
                      - $ref: "#/components/schemas/EmbeddingBenchmarkFailure"
    EmbeddingBenchmarkSuccess:
      type: object
      required:
        - model
        - dimensions
        - firstRequestMs
        - warmMs
        - retrievalCorrect
        - retrievalTotal
        - retrievalMs
        - batchCount
        - batchMs
        - batchPerItemMs
      properties:
        model: { type: string }
        dimensions: { type: integer, minimum: 1 }
        firstRequestMs: { type: number, minimum: 0 }
        warmMs: { type: number, minimum: 0 }
        retrievalCorrect: { type: integer, minimum: 0 }
        retrievalTotal: { type: integer, minimum: 1 }
        retrievalMs: { type: number, minimum: 0 }
        batchCount: { type: integer, minimum: 1 }
        batchMs: { type: number, minimum: 0 }
        batchPerItemMs: { type: number, minimum: 0 }
    EmbeddingBenchmarkFailure:
      type: object
      required: [model, error]
      properties:
        model: { type: string }
        error: { type: string }
    BenchmarkModelListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [models, providers]
          properties:
            models:
              type: array
              items:
                type: object
                required: [id, name, provider, available]
                properties:
                  id: { type: string }
                  name: { type: string }
                  provider: { type: string, enum: [github, ollama, gemini, anthropic] }
                  available: { type: boolean }
            providers:
              type: object
              additionalProperties:
                type: boolean
    BenchmarkRunListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [runs]
          properties:
            runs:
              type: array
              items:
                $ref: "#/components/schemas/BenchmarkRun"
    BenchmarkRun:
      type: object
      required: [run_id, model, provider, created_at, tests]
      properties:
        run_id: { type: string, format: uuid }
        model: { type: string }
        provider: { type: string }
        created_at: { type: string, format: date-time }
        tests:
          type: array
          items:
            type: object
            required: [prompt_name, prompt_label, total_ms, chars_generated]
            properties:
              prompt_name: { type: string }
              prompt_label: { type: string }
              ttft_ms: { type: [number, "null"] }
              total_ms: { type: number }
              chars_generated: { type: integer }
              tokens_generated: { type: [integer, "null"] }
              tokens_per_second: { type: [number, "null"] }
    CreateJobRequest:
      type: object
      required: [name, cronExpr, prompt, channels]
      properties:
        name:
          type: string
          minLength: 1
        cronExpr:
          type: string
          description: Standard 5-field cron expression
        prompt:
          type: string
          minLength: 1
        channels:
          type: array
          items:
            type: string
          minItems: 1
        enabled:
          type: boolean
          default: true
        domainSlug:
          type: [string, "null"]
        llmModel:
          type: [string, "null"]
          description: Explicit model ID. Null uses automatic selection.
        llmProvider:
          type: [string, "null"]
          enum: [github, ollama, gemini, anthropic, null]
          description: Must be provided together with llmModel.
    UpdateJobRequest:
      type: object
      properties:
        name:
          type: string
        cronExpr:
          type: string
        prompt:
          type: string
        channels:
          type: array
          items:
            type: string
        enabled:
          type: boolean
        llmModel:
          type: [string, "null"]
          description: Set both model fields to null to restore automatic selection.
        llmProvider:
          type: [string, "null"]
          enum: [github, ollama, gemini, anthropic, null]
    ScheduledJob:
      type: object
      required: [id, name, cronExpr, prompt, channels, domainSlug, llmModel, llmProvider, enabled, lastRunAt, createdAt, updatedAt]
      properties:
        id:
          type: string
        name:
          type: string
        cronExpr:
          type: string
        prompt:
          type: string
        channels:
          type: array
          items:
            type: string
        domainSlug:
          type: [string, "null"]
        llmModel:
          type: [string, "null"]
        llmProvider:
          type: [string, "null"]
          enum: [github, ollama, gemini, anthropic, null]
        enabled:
          type: boolean
        lastRunAt:
          type: [string, "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    JobExecution:
      type: object
      required: [id, jobId, status, result, startedAt, completedAt]
      properties:
        id:
          type: string
        jobId:
          type: string
        status:
          type: string
          enum: [running, completed, failed]
        result:
          type: [string, "null"]
        startedAt:
          type: string
          format: date-time
        completedAt:
          type: [string, "null"]
          format: date-time
    SkillListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [skills]
          properties:
            skills:
              type: array
              items:
                $ref: "#/components/schemas/Skill"
    SkillResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [skill]
          properties:
            skill:
              $ref: "#/components/schemas/Skill"
    SkillDetailResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [skill]
          properties:
            skill:
              $ref: "#/components/schemas/SkillDetail"
    CreateSkillRequest:
      type: object
      required: [name, displayName, description, systemPrompt]
      properties:
        name:
          type: string
          minLength: 1
        displayName:
          type: string
          minLength: 1
        description:
          type: string
        systemPrompt:
          type: string
        category:
          type: string
          default: workflow
        shared:
          type: boolean
          default: false
    UpdateSkillRequest:
      type: object
      properties:
        name:
          type: string
        displayName:
          type: string
        description:
          type: string
        systemPrompt:
          type: string
        category:
          type: string
        shared:
          type: boolean
    Skill:
      type: object
      required: [id, name, displayName, description, category, shared, verified, createdAt, updatedAt]
      properties:
        id:
          type: string
        name:
          type: string
        displayName:
          type: string
        description:
          type: string
        category:
          type: string
        shared:
          type: boolean
        verified:
          type: boolean
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
    SkillDetail:
      allOf:
        - $ref: "#/components/schemas/Skill"
        - type: object
          required: [systemPrompt]
          properties:
            systemPrompt:
              type: string
    HealthStatusResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [status]
          properties:
            status:
              $ref: "#/components/schemas/HealthStatus"
    HealthStatus:
      type: object
      required: [isConnected, mfaPending, syncEnabled, lastSyncAt, lastError]
      properties:
        isConnected:
          type: boolean
        mfaPending:
          type: boolean
        syncEnabled:
          type: boolean
        lastSyncAt:
          type: [string, "null"]
          format: date-time
        lastError:
          type: [string, "null"]
    HealthSummaryResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [summary]
          properties:
            summary:
              $ref: "#/components/schemas/HealthSummary"
    HealthSummary:
      type: object
      required: [period]
      properties:
        period:
          type: string
          enum: [day, 3days, week, month, year]
        avgSteps:
          type: [integer, "null"]
        avgCalories:
          type: [integer, "null"]
        avgRestingHr:
          type: [integer, "null"]
        avgSleepHours:
          type: [number, "null"]
        totalSteps:
          type: [integer, "null"]
        totalCalories:
          type: [integer, "null"]
        maxSteps:
          type: [integer, "null"]
        daysWithData:
          type: integer
    DailyHealthResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [daily]
          properties:
            daily:
              anyOf:
                - $ref: "#/components/schemas/DailyHealth"
                - type: "null"
    DailyHealth:
      type: object
      required: [date]
      properties:
        date:
          type: string
          format: date
        steps:
          type: [integer, "null"]
        calories:
          type: [integer, "null"]
        restingHr:
          type: [integer, "null"]
        sleepDurationMinutes:
          type: [integer, "null"]
        sleepScore:
          type: [integer, "null"]
        bodyBatteryMin:
          type: [integer, "null"]
        bodyBatteryMax:
          type: [integer, "null"]
        stressAvg:
          type: [integer, "null"]
    HealthActivitiesResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [activities]
          properties:
            activities:
              type: array
              items:
                type: object
                additionalProperties: true
    HealthActivityDetailResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [activity]
          properties:
            activity:
              type: object
              additionalProperties: true
    HealthActivityLapsResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [activityId, laps]
          properties:
            activityId:
              type: string
            laps:
              type: array
              items:
                type: object
                additionalProperties: true
    HealthActivityZonesResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [activityId, zones]
          properties:
            activityId:
              type: string
            zones:
              type: array
              items:
                type: object
                additionalProperties: true
    HealthActivitySyncResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [activityId, queued, jobStatus]
          properties:
            activityId:
              type: string
            queued:
              type: boolean
            jobStatus:
              type: string
              enum: [pending, claimed, running, completed, failed]
    HealthActivityDeleteResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [activityId, deleted]
          properties:
            activityId:
              type: string
            deleted:
              type: boolean
    HealthActivityRouteResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [activityId, bounds]
          properties:
            activityId:
              type: string
            bounds:
              type: object
              additionalProperties: true
            simplifiedPolyline:
              type: string
              nullable: true
            sampleCount:
              type: integer
              nullable: true
            coordinates:
              type: array
              items:
                type: object
                additionalProperties: true
            redacted:
              type: boolean
    HealthActivitySeriesResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [activityId, metrics, points]
          properties:
            activityId:
              type: string
            metrics:
              type: array
              items:
                type: string
            redacted:
              type: boolean
            totalSamples:
              type: integer
            points:
              type: array
              items:
                type: object
                additionalProperties: true
    HealthProtectedLocationInput:
      type: object
      required: [lat, lng, radiusMeters]
      properties:
        label:
          type: string
          nullable: true
        lat:
          type: number
          minimum: -90
          maximum: 90
        lng:
          type: number
          minimum: -180
          maximum: 180
        radiusMeters:
          type: number
          minimum: 0
          exclusiveMinimum: true
    HealthProtectedLocation:
      type: object
      required: [id, lat, lng, radiusMeters]
      properties:
        id:
          type: string
        label:
          type: string
          nullable: true
        lat:
          type: number
        lng:
          type: number
        radiusMeters:
          type: number
    HealthProtectedLocationsResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [locations]
          properties:
            locations:
              type: array
              items:
                $ref: "#/components/schemas/HealthProtectedLocation"
    HealthProtectedLocationResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [location]
          properties:
            location:
              $ref: "#/components/schemas/HealthProtectedLocation"
    HealthProtectedLocationDeleteResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [id, deleted]
          properties:
            id:
              type: string
            deleted:
              type: boolean
    HealthProxyMeta:
      type: object
      properties:
        connector:
          type: string
          example: garmin
        operation:
          type: string
        dataMode:
          type: string
          enum: [proxy]
        stored:
          type: boolean
          enum: [false]
    HealthProxyStatusResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [status, meta]
          properties:
            status:
              type: object
              properties:
                isConnected:
                  type: boolean
                mfaPending:
                  type: boolean
                syncEnabled:
                  type: boolean
            meta:
              $ref: "#/components/schemas/HealthProxyMeta"
    HealthProxyActivitiesResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [activities, meta]
          properties:
            activities:
              type: array
              description: Raw Garmin activity objects, as returned by the provider — never normalized.
              items:
                type: object
                additionalProperties: true
            meta:
              $ref: "#/components/schemas/HealthProxyMeta"
    HealthProxyActivityDetailResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          description: >-
            The health-worker's raw /activity-details payload passed through untouched,
            plus a meta marker. Includes exact GPS coordinates (samples) and details_raw
            (Garmin's complete unreduced payload) — treat as sensitive.
          additionalProperties: true
    HealthProxyDailyResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [daily, meta]
          properties:
            daily:
              type: object
              description: Raw Garmin daily-health payload, as returned by the provider — never normalized.
              additionalProperties: true
            meta:
              $ref: "#/components/schemas/HealthProxyMeta"
    WebSearchResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [search]
          properties:
            search:
              $ref: "#/components/schemas/WebSearchResult"
    WebSearchResult:
      type: object
      required: [results, query]
      properties:
        answer:
          type: string
        results:
          type: array
          items:
            $ref: "#/components/schemas/WebSearchItem"
        query:
          type: string
        from_cache:
          type: boolean
        cached_at:
          type: string
          format: date-time
        error:
          type: string
    WebSearchItem:
      type: object
      required: [title, url, content]
      properties:
        title:
          type: string
        url:
          type: string
          format: uri
        content:
          type: string
        score:
          type: number
    EmailMessageListResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [messages]
          properties:
            messages:
              type: array
              items:
                $ref: "#/components/schemas/EmailSummary"
    EmailMessageResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [message]
          properties:
            message:
              $ref: "#/components/schemas/EmailDetail"
    SendEmailResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [sent]
          properties:
            sent:
              type: boolean
              const: true
    SendEmailRequest:
      type: object
      required: [accountId, to, subject, body]
      properties:
        accountId:
          type: string
        to:
          type: string
        subject:
          type: string
        body:
          type: string
        inReplyTo:
          type: string
        references:
          type: string
    EmailSummary:
      type: object
      required: [uid, messageId, subject, from, fromName, date, snippet, seen]
      properties:
        uid:
          type: integer
        messageId:
          type: string
        subject:
          type: string
        from:
          type: string
        fromName:
          type: string
        date:
          type: string
        snippet:
          type: string
        seen:
          type: boolean
    EmailDetail:
      allOf:
        - $ref: "#/components/schemas/EmailSummary"
        - type: object
          required: [to, cc, bodyText, bodyHtml]
          properties:
            to:
              type: string
            cc:
              type: string
            bodyText:
              type: string
            bodyHtml:
              type: string
    FinanceQuotesResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [quotes]
          properties:
            quotes:
              type: array
              items:
                $ref: "#/components/schemas/FinanceQuote"
    FinanceSymbolSearchResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [result]
          properties:
            result:
              type: array
              items:
                $ref: "#/components/schemas/FinanceSymbolSearchResult"
    FinanceCandlesResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [candles]
          properties:
            candles:
              type: array
              items:
                $ref: "#/components/schemas/FinanceCandle"
    FinanceQuote:
      type: object
      required: [symbol]
      properties:
        symbol:
          type: string
        name:
          type: string
        c:
          type: number
          description: Current price
        d:
          type: number
          description: Absolute change versus previous close
        dp:
          type: number
          description: Percent change versus previous close
        error:
          type: boolean
    FinanceSymbolSearchResult:
      type: object
      required: [symbol, displaySymbol, description, type]
      properties:
        symbol:
          type: string
        displaySymbol:
          type: string
        description:
          type: string
        type:
          type: string
    FinanceCandle:
      type: object
      required: [t, c]
      properties:
        t:
          type: integer
          description: Unix timestamp in milliseconds
        c:
          type: number
          description: Close price
    WatchlistResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [symbols]
          properties:
            symbols:
              type: array
              items:
                type: string
    WatchlistSymbolResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [symbol]
          properties:
            symbol:
              type: string
            added:
              type: boolean
            deleted:
              type: boolean
    AddWatchlistSymbolRequest:
      type: object
      required: [symbol]
      properties:
        symbol:
          type: string
          minLength: 1
    MusicStatusResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [status]
          properties:
            status:
              type: object
              required: [configured, is_connected]
              properties:
                configured:
                  type: boolean
                is_connected:
                  type: boolean
                display_name:
                  type: string
                  nullable: true
                avatar_url:
                  type: string
                  nullable: true
                scopes:
                  type: string
                  nullable: true
                access_expires_at:
                  type: string
                  format: date-time
                  nullable: true
                last_sync_at:
                  type: string
                  format: date-time
                  nullable: true
                last_error:
                  type: string
                  nullable: true
    MusicArtist:
      type: object
      required: [id, name]
      properties:
        id:
          type: string
        name:
          type: string
    MusicRecommendation:
      type: object
      required: [trackId, score, name, artists]
      properties:
        trackId:
          type: string
        score:
          type: number
        reason:
          type: string
          nullable: true
        name:
          type: string
        artists:
          type: array
          items:
            $ref: "#/components/schemas/MusicArtist"
        albumName:
          type: string
          nullable: true
        albumImageUrl:
          type: string
          nullable: true
        externalUrl:
          type: string
          nullable: true
        previewUrl:
          type: string
          nullable: true
    MusicRecommendationsResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [recommendations]
          properties:
            recommendations:
              type: array
              items:
                $ref: "#/components/schemas/MusicRecommendation"
    MusicTopTrack:
      type: object
      required: [trackId, name, artists]
      properties:
        trackId:
          type: string
        name:
          type: string
        artists:
          type: array
          items:
            $ref: "#/components/schemas/MusicArtist"
        albumImageUrl:
          type: string
          nullable: true
        externalUrl:
          type: string
          nullable: true
        rank:
          type: integer
          nullable: true
    MusicTopTracksResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [period, tracks]
          properties:
            period:
              type: string
              enum: [top_short, top_medium, top_long]
            tracks:
              type: array
              items:
                $ref: "#/components/schemas/MusicTopTrack"
    MusicRecentlyPlayedTrack:
      type: object
      required: [trackId, name, artists]
      properties:
        trackId:
          type: string
        name:
          type: string
        artists:
          type: array
          items:
            $ref: "#/components/schemas/MusicArtist"
        albumImageUrl:
          type: string
          nullable: true
        externalUrl:
          type: string
          nullable: true
        playedAt:
          type: string
          format: date-time
          nullable: true
    MusicRecentlyPlayedResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [tracks]
          properties:
            tracks:
              type: array
              items:
                $ref: "#/components/schemas/MusicRecentlyPlayedTrack"
    MusicPlaylist:
      type: object
      required: [id, name, trackCount]
      properties:
        id:
          type: string
        name:
          type: string
        imageUrl:
          type: string
          nullable: true
        trackCount:
          type: integer
        externalUrl:
          type: string
          nullable: true
    MusicPlaylistsResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [playlists]
          properties:
            playlists:
              type: array
              items:
                $ref: "#/components/schemas/MusicPlaylist"
    CreateMusicPlaylistRequest:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 1
          maxLength: 200
        trackIds:
          type: array
          items:
            type: string
          maxItems: 100
    CreateMusicPlaylistResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [playlist]
          properties:
            playlist:
              type: object
              required: [id, name]
              properties:
                id:
                  type: string
                name:
                  type: string
                externalUrl:
                  type: string
                  nullable: true
    MusicPlaylistTrack:
      type: object
      required: [trackId, name, artists]
      properties:
        trackId:
          type: string
        name:
          type: string
        artists:
          type: array
          items:
            $ref: "#/components/schemas/MusicArtist"
        albumImageUrl:
          type: string
          nullable: true
        externalUrl:
          type: string
          nullable: true
        addedAt:
          type: string
          format: date-time
          nullable: true
    MusicPlaylistTracksResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [tracks]
          properties:
            tracks:
              type: array
              items:
                $ref: "#/components/schemas/MusicPlaylistTrack"
    AddMusicPlaylistTracksRequest:
      type: object
      required: [trackIds]
      properties:
        trackIds:
          type: array
          items:
            type: string
          minItems: 1
          maxItems: 100
    AddMusicPlaylistTracksResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [added]
          properties:
            added:
              type: integer
    MusicSyncResponse:
      type: object
      required: [data]
      properties:
        data:
          type: object
          required: [result]
          properties:
            result:
              type: object
              required: [tracksUpserted, historyInserted, candidatesDiscovered, recommendationsGenerated]
              properties:
                tracksUpserted:
                  type: integer
                historyInserted:
                  type: integer
                candidatesDiscovered:
                  type: integer
                recommendationsGenerated:
                  type: integer
    SpeechRequest:
      type: object
      required: [text]
      properties:
        text:
          type: string
          minLength: 1
          maxLength: 4096
        voice:
          type: string
          minLength: 1
          maxLength: 80
        model:
          type: string
          minLength: 1
          maxLength: 120
        speed:
          type: number
          minimum: 0.25
          maximum: 4
    RobotSpeechSettings:
      type: object
      required: [voice, speed, style]
      properties:
        voice:
          type: string
        speed:
          type: number
          minimum: 0.25
          maximum: 4
        style:
          type: string
    RobotSettingsUpdateRequest:
      allOf:
        - $ref: "#/components/schemas/RobotSpeechSettings"
    RobotSettingsUpdateResponse:
      type: object
      required: [data]
      properties:
        data:
          $ref: "#/components/schemas/RobotSpeechSettings"
    RobotSettingsResponse:
      type: object
      required: [data]
      properties:
        data:
          allOf:
            - $ref: "#/components/schemas/RobotSpeechSettings"
            - type: object
              required: [voices, defaultSkill, tools]
              properties:
                voices:
                  type: array
                  items:
                    type: string
                defaultSkill:
                  oneOf:
                    - $ref: "#/components/schemas/RobotDefaultSkill"
                    - type: "null"
                tools:
                  type: array
                  items:
                    $ref: "#/components/schemas/RobotTool"
    RobotDefaultSkill:
      type: object
      required: [id, name, displayName]
      properties:
        id:
          type: string
        name:
          type: string
        displayName:
          type: string
    RobotTool:
      type: object
      required: [name, label, description, group, runtimeAvailable]
      properties:
        name:
          type: string
        label:
          type: string
        description:
          type: string
        group:
          type: string
        runtimeAvailable:
          type: boolean
    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
            message:
              type: string
            details:
              description: Optional validation or diagnostic details.
