meta {
  name: List Ticket Events
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/v1/tickets/{{ticketId}}/events
  body: none
  auth: none
}

headers {
  Cookie: session_token={{sessionToken}}
}
