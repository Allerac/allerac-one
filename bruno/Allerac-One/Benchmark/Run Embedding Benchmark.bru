meta {
  name: Run Embedding Benchmark
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/benchmark/embeddings
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{apiKey}}
}

body:json {
  {
    "models": [
      "embeddinggemma",
      "nomic-embed-text-v2-moe"
    ]
  }
}
