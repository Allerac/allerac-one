meta {
  name: Update Note
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/api/v1/notes/{{noteId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  Authorization: Bearer {{apiKey}}
}

body:json {
  {
    "tags": ["api", "bruno", "updated"]
  }
}
